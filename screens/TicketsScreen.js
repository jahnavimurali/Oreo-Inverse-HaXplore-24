import React from "react";
import {
  View,
  TextInput,
  Text,
  Button,
  StyleSheet,
  TouchableOpacity,
} from "react-native";

export default function TicketsScreen({ navigation }) {
  return <Text> TicketsScreen </Text>;
}
